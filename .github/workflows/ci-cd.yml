name: Frontend CI (minimal)

on:
  push:
    branches:
      - main
      - develop
      - feature/*
  pull_request:
    branches:
      - main
      - develop
      - feature/*

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout do código
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        # mesma versão do deploy.yml e da EC2, o workbox do PWA não roda no node 18
        node-version: '20'
        cache: npm

    - name: Instalar dependências
      run: |
        if [ -f package-lock.json ]; then
          npm ci
        else
          npm install
        fi

    - name: Build (Vite)
      run: npm run build
